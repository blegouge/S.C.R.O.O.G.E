---
description: >-
  Diff-Only Protocol — code changes in chat/subagents as SEARCH/REPLACE blocks only;
  no full-file or unchanged-code output. Use for implementation, refactors, patches.
alwaysApply: true
---

# Diff-Only Protocol

**Canonical spec:** `src/rules/diff_protocol.md` (SSOT — SEARCH/REPLACE format, special cases, applier integration).

## Output (mandatory for any code change shown to user or parent agent)

- **Never** print intact/unchanged code, whole files, or “updated file” dumps.
- **Only** hunks in this exact shape (repeat per hunk):

```text
path: <relative/path>
<<<<<<< SEARCH
<exact existing snippet>
=======
<replacement snippet>
>>>>>>> REPLACE
```

## Matching

- SEARCH = **verbatim** from disk (indentation, quotes, EOL).
- Add minimal surrounding lines so the match is **unique**.
- New file / delete: see `src/rules/diff_protocol.md` § Special cases.

## Subagents

Brief must include: *« Follow Diff-Only: return path + SEARCH/REPLACE blocks only; no file dumps. »*

## Edits in repo (enforced)

- **Primary path (Diff-Only):** put `path:` + SEARCH/REPLACE hunks in the assistant reply — `diff-only-apply.py` writes them on `afterAgentResponse` / `subagentStop`.
- **Mid-turn tools:** use `StrReplace` / `ApplyPatch` / `Edit` when available. Cursor may label them `Write`; the preToolUse guard soft-allows full-file Write by default (hard deny only if `CURSOR_DIFF_ONLY_STRICT_WRITE=1`).
- **Mandatory response fallback:** if a tool fails, emit exact SEARCH/REPLACE blocks in the response. Do not stop solely because an editor looked blocked.
- **Shell Bypass Forbidden:** Do NOT use shell (`cat`, `sed`, `tee`, redirection) to create/edit repo files.
- **New files:** `Write` allowed; later edits follow the policy above.
- Parent/subagent briefs must state: « Diff-Only: SEARCH/REPLACE in reply or targeted editor; no full-file dumps ».

## Exceptions

- User explicitly asks for full file, diff, or pedagogy (*détail*, *complet*).
- Non-code answers: no blocks.
